name: go_enrichment
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Python stack
  - python=3.11
  - pandas
  - matplotlib
  - wget

  # R + Bioconductor
  - r-base
  - bioconductor-topgo

  # GO utilities
  - goatools
